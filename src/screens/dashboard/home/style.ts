import { deviceWidth, hp } from 'constants/layout';
import { StyleSheet } from 'react-native';
import theme from 'theme';

export const styles = StyleSheet.create({
  dashboardImageContainer: {
    width: deviceWidth,
    height: hp(376),
  },
});
